import 'package:flutter/material.dart';

class AuthorWordsScreen extends StatelessWidget {
  const AuthorWordsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    appBar: AppBar(),
    );
  }
}
